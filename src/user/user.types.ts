export interface OpenPosition {
  details: string;
  startDate: string;
  positionSize: string;
  betOnTeam: string;
}

export interface WinningPosition {
  details: string;
  amount: string;
  address: string;
}
