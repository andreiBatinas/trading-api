const path = require("path");

module.exports = {
  config: path.resolve("sequelize", "config", "migration.config.js"),
  // "models-path": path.resolve(),
  "migrations-path": path.resolve("sequelize", "migrations"),
};
